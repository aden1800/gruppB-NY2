@model IEnumerable<gruppBNY.Models.core_abilities>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ability)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ability)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.core_abilities_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.core_abilities_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.core_abilities_Id })
        </td>
    </tr>
}

</table>
