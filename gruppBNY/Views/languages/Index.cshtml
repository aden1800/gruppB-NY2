@model IEnumerable<gruppBNY.Models.languages>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.languages1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.languages1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.languages_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.languages_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.languages_Id })
        </td>
    </tr>
}

</table>
