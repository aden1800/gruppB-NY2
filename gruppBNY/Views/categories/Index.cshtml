@model IEnumerable<gruppBNY.Models.category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.type_of_competence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.type_of_competence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.category_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.category_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.category_Id })
        </td>
    </tr>
}

</table>
